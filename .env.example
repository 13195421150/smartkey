# Run: bash scripts/init-env.sh
# Then set your own domain, without https:// or a path.
CDK_DOMAIN=
JWT_SECRET=
SETUP_BOOTSTRAP_TOKEN=
INSTALL_MODE=wizard
UPDATE_ENABLED=0

# Optional. Recommended: configure your own API in /ops/integration after setup.
CARD_API_BASE=
CARD_API_KEY=
